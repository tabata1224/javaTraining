package consallink.chapter4;

public class Training4_2 {

    public int practice1() {

        // 問題1
        // インスタンスに関する説明として正しい説明を選んでください
        // 1.同一インスタンスかどうかを比較する場合は比較演算子「==」を利用する
        // 2.インスタンス変数名が異なれば、必ず別のインスタンスとなる
        // 3.インスタンス変数は宣言時、フィールドもしくはローカル変数に関係なく、自動的にnullが代入される

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 1;

        return answer;
    }

    public int practice2() {

        // 問題2
        // コンストラクタに関する説明として正しい説明を選んでください
        // 1.コンストラクタ定義時はクラス名以外の名称を指定できる
        // 2.コンストラクタ定義時は必ず引数を一つ以上指定する必要がある
        // 3.コンストラクタを定義する際は戻り値を指定できる
        // 4.コンストラクタを定義しなかった場合、デフォルトコンストラクタが自動的に生成される

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 4;

        return answer;
    }

    public int practice3() {

        // 問題3
        // コンストラクタに関する説明として正しい説明を選んでください
        // 1.コンストラクタはオーバーロード可能である
        // 2.クラス定義時は必ずコンストラクタを定義する必要がある
        // 3.コンストラクタはインスタンス生成後、いつでも呼び出して実行できる
        // 4.コンストラクタにはアクセス修飾子を付加できない

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 1;

        return answer;
    }

    public int practice4() {

        // 問題4
        // ABCのコンストラクタを定義する際の正しい記述を選んでください
        // 1.void ABC(){}
        // 2.abc(){}
        // 3.ABC(){}
        // 4.void abc(){}

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 3;

        return answer;
    }

    public int practice5() {

        // 問題5
        // オーバーロードに関する説明として正しい説明を選んでください
        // 1.オーバーロードは戻り値の型を変更することで同一名のメソッドを定義することができることを指す
        // 2.「void method(int a, int b)」と「void method(int c, int
        // d)」が定義されている場合、オーバーロードとして定義できる
        // 3.「void method(int a, int b)」と「void method(int b, int
        // a)」が定義されている場合、オーバーロードとして定義できる
        // 4.オーバーロードとしてメソッドを定義する場合は、引数の型、数、型の並び、のいずれかが異なる必要がある

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 4;

        return answer;
    }

    public int practice6() {

        // 問題6
        // void method()のメソッドが定義されている場合、オーバーロードとなるメソッド定義を選んでください
        // 1.int method()
        // 2.double method()
        // 3.void method(String str)
        // 4.void method()

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 3;

        return answer;
    }

}
