package consallink.chapter3;

public class Training3_1 {

    public int practice1() {

        // 問題1
        // 制御構文の説明として間違っているものを選んでください
        // 1.制御構文を利用しないプログラムの流れは、記述された流れを逐次実行する為「逐次処理」と呼ばれる
        // 2.条件によってプログラムの流れを変更する処理は「分岐処理」と呼ばれ、ifやswitchがある
        // 3.プログラムの流れを何度も繰り返し行う処理を「反復処理」と呼び、forやwhileがある
        // 4.プログラムの処理順序を入れ替えたり上書きしたりする処理を「継続処理」と呼び、breakやcontinueがある

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 4;

        return answer;
    }

    public int practice2() {

        // 問題2
        // if(条件){} の条件部分に記述できる処理として間違っているものを選んでください
        // 1.3 > 5
        // 2.5 == 5
        // 3.int num = 5 + 5
        // 4.3 > 5 && 5 > 5

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 3;

        return answer;
    }

    public int practice3() {

        // 問題3
        // switch(式){ の式に記述できる型として間違っているものを選んでください
        // 1.char型
        // 2.short型
        // 3.int型
        // 4.long型

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 4;

        return answer;
    }

    public int practice4() {

        // 問題4
        // ifの説明として間違っているものを選んでください
        // 1.if-else if文では必ずすべての条件が確認（実行）される
        // 2.if-else if文では上から順に条件が確認（実行）される
        // 3.ifやelse ifでの条件はboolean型の変数（もしくは演算結果）を利用できる
        // 4.ifやelse ifでの条件はboolean型の値を利用できる

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 1;

        return answer;
    }

    public int practice5() {

        // 問題5
        // switchの説明として間違っているものを選んでください（numberはint型とします）
        // 1.式には算術演算子を記述できる【例：switch(number + 5)】
        // 2.式には代入演算子を記述できる【例：switch(number = 5)】
        // 3.式には比較演算子を記述できる【例：switch(number > 5)】
        // 4.式にはインクリメントを記述できる【例：switch(number++)】

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 3;

        return answer;
    }

    public int practice6() {

        // 問題6
        // 反復処理（ループ処理）の説明として間違っているものを選んでください
        // 1.for文は一回も実行されないことがある
        // 2.while文は一回も実行されないことがある
        // 3.do-while文は一回も実行されないことがある
        // 4.反復処理内にはbreakやcontinueを記述できる

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 3;

        return answer;
    }

    public int practice7() {

        // 問題7
        // breakの説明として間違っているものを選んでください
        // 1.forやwhileなどのループ文だけでなくswitch文でも記述できる
        // 2.switch文だけでなくif文でも記述できる
        // 3.ループ内のbreakに到達すると、ループは終了する
        // 4.switch文中のbreakは記述しなくてもエラーにならない

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 2;

        return answer;
    }

    public int practice8() {

        // 問題8
        // cotinueの説明として間違っているものを選んでください
        // 1.forやwhileなどのループ文で記述可能で、switch文では記述できない
        // 2.breakと同様にループを終了させることができる
        // 3.continueに到達するとその後の処理を中断し、ループ文の先頭に戻る
        // 4.for文中のcontinueに到達すると、増減式に処理が移る

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 2;

        return answer;
    }

    public int practice9() {

        // 問題9
        // 次のfor文で無限ループ（ループが終了しない）になるものを選んでください
        // 1.for(int i = 0; i < 100; i++){
        // 2.for(int i = 0; i < 100; i+=2){
        // 3.for(int i = 100; i > 0; i++){
        // 4.for(int i = 100; i > 0; i–){

        // 変数answerに答えの選択肢の数値を代入しよう！
        int answer = 3;

        return answer;
    }
}
